git config --global user.name "Daniel Cabral"
git config --global user.email "daniel-cabral14@hotmail.com"
git config --global color.ui true
git status
git add arquivo.txt
git commit -m "Meu Primeiro Commit"
git log
git commit
git add .
git commit -a "Descricao"
git checkout -- nome do arquivo //remover arquivo da lista de commit
git log -p //para cada arquivo, o que foi modificado
git log -p -2 //apenas para os 2 ultimos commits
git log --stat //estatisticas dos commits
git log --pretty=oneline //todos os commits em uma linha só
git log --prety=format:"%h - %an, %ar : %s" //codigo, nome, tempo e titulo
git log --since=2.days //commits dos ultimos 2 dias
.gitignore
git reset HEAD nomeDoArquivo
git checkout hash
git branch
git reset HEAD~n //volta n commits
git reset HEAD~n --soft //volta n commits e deixa eles prontos para serem commitados
	--hard //apaga os commits e arquivos
git checkout -b funcionalidade1


